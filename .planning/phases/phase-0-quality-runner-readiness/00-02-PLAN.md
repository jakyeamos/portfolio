# 00-02 Plan: Quality Runner Smoke Baseline

## Goal

Test the lowest-risk Quality Runner commands available locally and record whether
the tool is ready to run against candidate repos without dependency work.

## Target Repos

- `/Users/jakyeamos/quality-runner`
- `/Users/jakyeamos/projects/portfolio`

## Deliverable

- `.planning/interview-surface/quality-runner-smoke-baseline.md`

## Steps

- [ ] Run `command -v quality-runner`.
- [ ] If installed, run `quality-runner --version` and `quality-runner doctor --json`.
- [ ] If installed, run `quality-runner inspect /Users/jakyeamos/projects/portfolio --profile jakyeamos --json`.
- [ ] If not installed, try `uv run quality-runner doctor --json` from `/Users/jakyeamos/quality-runner`.
- [ ] If dependency or network setup blocks execution, stop and record the blocker; do not install packages in this phase.
- [ ] Save command results and interpretation in the smoke baseline.

## Commit Strategy

Do not commit generated `.quality-runner/` artifacts unless a reviewer explicitly
decides they are useful planning evidence.
